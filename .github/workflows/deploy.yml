name: Deploy to Firebase Hosting

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy-qr-code:
    name: Deploy QR Code Generator
    runs-on: ubuntu-latest
    if: >
      github.event_name == 'workflow_dispatch' ||
      contains(toJson(github.event.commits), 'qr-code/')
    steps:
      - uses: actions/checkout@v4

      - name: Generate config.js
        run: |
          mkdir -p qr-code/public/js
          cat > qr-code/public/js/config.js << 'EOF'
          const firebaseConfig = {
            apiKey: "${{ secrets.FIREBASE_API_KEY }}",
            authDomain: "${{ secrets.FIREBASE_AUTH_DOMAIN }}",
            projectId: "${{ secrets.FIREBASE_PROJECT_ID }}",
            storageBucket: "${{ secrets.FIREBASE_STORAGE_BUCKET }}",
            messagingSenderId: "${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}",
            appId: "${{ secrets.FIREBASE_APP_ID }}",
            measurementId: "${{ secrets.FIREBASE_MEASUREMENT_ID }}"
          };
          firebase.initializeApp(firebaseConfig);

          const EMAILJS_PUBLIC_KEY = "${{ secrets.EMAILJS_PUBLIC_KEY }}";
          const EMAILJS_SERVICE_ID = "${{ secrets.EMAILJS_SERVICE_ID }}";
          const EMAILJS_TEMPLATE_ID = "${{ secrets.EMAILJS_TEMPLATE_ID }}";
          emailjs.init(EMAILJS_PUBLIC_KEY);

          window.GEMINI_API_KEY = "${{ secrets.GEMINI_API_KEY }}";
          EOF

      - name: Deploy to Firebase
        uses: w9jds/firebase-action@master
        with:
          args: deploy --only hosting --project create-your-qr
        env:
          GCP_SA_KEY: ${{ secrets.GCP_SA_KEY }}
          PROJECT_PATH: ./qr-code

  deploy-image-resizer:
    name: Deploy Image Resizer
    runs-on: ubuntu-latest
    if: >
      github.event_name == 'workflow_dispatch' ||
      contains(toJson(github.event.commits), 'image-resizer/')
    steps:
      - uses: actions/checkout@v4

      - name: Generate config.js
        run: |
          mkdir -p image-resizer/public/js
          cat > image-resizer/public/js/config.js << 'EOF'
          const firebaseConfig = {
            apiKey: "${{ secrets.FIREBASE_API_KEY }}",
            authDomain: "${{ secrets.FIREBASE_AUTH_DOMAIN }}",
            projectId: "${{ secrets.FIREBASE_PROJECT_ID }}",
            storageBucket: "${{ secrets.FIREBASE_STORAGE_BUCKET }}",
            messagingSenderId: "${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}",
            appId: "${{ secrets.FIREBASE_APP_ID }}",
            measurementId: "${{ secrets.FIREBASE_MEASUREMENT_ID }}"
          };
          firebase.initializeApp(firebaseConfig);
          EOF

      - name: Deploy to Firebase
        uses: w9jds/firebase-action@master
        with:
          args: deploy --only hosting --project create-your-qr
        env:
          GCP_SA_KEY: ${{ secrets.GCP_SA_KEY }}
          PROJECT_PATH: ./image-resizer

  deploy-ems-tools:
    name: Deploy EMS Tools
    runs-on: ubuntu-latest
    if: >
      github.event_name == 'workflow_dispatch' ||
      contains(toJson(github.event.commits), 'ems-tools/')
    steps:
      - uses: actions/checkout@v4

      - name: Generate config.js
        run: |
          mkdir -p ems-tools/public/js
          cat > ems-tools/public/js/config.js << 'EOF'
          const firebaseConfig = {
            apiKey: "${{ secrets.FIREBASE_API_KEY }}",
            authDomain: "${{ secrets.FIREBASE_AUTH_DOMAIN }}",
            projectId: "${{ secrets.FIREBASE_PROJECT_ID }}",
            storageBucket: "${{ secrets.FIREBASE_STORAGE_BUCKET }}",
            messagingSenderId: "${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}",
            appId: "${{ secrets.FIREBASE_APP_ID }}",
            measurementId: "${{ secrets.FIREBASE_MEASUREMENT_ID }}"
          };
          firebase.initializeApp(firebaseConfig);
          EOF

      - name: Deploy to Firebase
        uses: w9jds/firebase-action@master
        with:
          args: deploy --only hosting --project create-your-qr
        env:
          GCP_SA_KEY: ${{ secrets.GCP_SA_KEY }}
          PROJECT_PATH: ./ems-tools

  deploy-tadda-portal:
    name: Deploy Tadda Portal
    runs-on: ubuntu-latest
    if: >
      github.event_name == 'workflow_dispatch' ||
      contains(toJson(github.event.commits), 'tadda/')
    steps:
      - uses: actions/checkout@v4

      - name: Generate config.js
        run: |
          mkdir -p tadda/public/js
          cat > tadda/public/js/config.js << 'EOF'
          const firebaseConfig = {
            apiKey: "${{ secrets.TADDA_FIREBASE_API_KEY }}",
            authDomain: "${{ secrets.TADDA_FIREBASE_AUTH_DOMAIN }}",
            projectId: "${{ secrets.TADDA_FIREBASE_PROJECT_ID }}",
            storageBucket: "${{ secrets.TADDA_FIREBASE_STORAGE_BUCKET }}",
            messagingSenderId: "${{ secrets.TADDA_FIREBASE_MESSAGING_SENDER_ID }}",
            appId: "${{ secrets.TADDA_FIREBASE_APP_ID }}",
            measurementId: "${{ secrets.TADDA_FIREBASE_MEASUREMENT_ID }}"
          };
          firebase.initializeApp(firebaseConfig);
          window.ADMIN_EMAIL = "${{ secrets.TADDA_ADMIN_EMAIL }}";
          window.WEB3FORMS_KEY = "${{ secrets.WEB3FORMS_KEY }}";
          EOF

      - name: Deploy to Firebase
        uses: w9jds/firebase-action@master
        with:
          args: deploy --only hosting --project tadda-81f3e
        env:
          GCP_SA_KEY: ${{ secrets.GCP_SA_KEY_TADDA }}
          PROJECT_PATH: ./tadda

  deploy-tadda-web:
    name: Deploy Tadda Marketing Website
    runs-on: ubuntu-latest
    if: >
      github.event_name == 'workflow_dispatch' ||
      contains(toJson(github.event.commits), 'tadda-web/')
    steps:
      - uses: actions/checkout@v4

      - name: Generate config.js
        run: |
          mkdir -p tadda-web/public/js
          cat > tadda-web/public/js/config.js << 'EOF'
          const firebaseConfig = {
            apiKey: "${{ secrets.TADDA_FIREBASE_API_KEY }}",
            authDomain: "${{ secrets.TADDA_FIREBASE_AUTH_DOMAIN }}",
            projectId: "${{ secrets.TADDA_FIREBASE_PROJECT_ID }}",
            storageBucket: "${{ secrets.TADDA_FIREBASE_STORAGE_BUCKET }}",
            messagingSenderId: "${{ secrets.TADDA_FIREBASE_MESSAGING_SENDER_ID }}",
            appId: "${{ secrets.TADDA_FIREBASE_APP_ID }}"
          };
          firebase.initializeApp(firebaseConfig);
          window.WEB3FORMS_KEY = "${{ secrets.WEB3FORMS_KEY }}";
          EOF

      - name: Deploy to Firebase
        uses: w9jds/firebase-action@master
        with:
          args: deploy --only hosting --project tadda-81f3e
        env:
          GCP_SA_KEY: ${{ secrets.GCP_SA_KEY_TADDA }}
          PROJECT_PATH: ./tadda-web
